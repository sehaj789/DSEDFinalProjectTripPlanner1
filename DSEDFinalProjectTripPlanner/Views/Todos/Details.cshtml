@model DSEDFinalProjectTripPlanner.DTO.TodoDTO

@{
    ViewData["Title"] = "Details";
}

<h2 style="font-family:cursive; font-size:24px;">@Html.DisplayFor(model => model.MyTodos.Title)</h2>

<div>
    <h4 style="font-family:cursive; font-size:24px;">ToDo</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MyTodos.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.MyTodos.Title)
        </dd>
    </dl>
</div>

<div class="col-md-12">
 
    @Html.Partial("~/Views/Items/Create.cshtml", new Item())
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model.MyTodos.Id" class="btn btn-info">Edit</a> |
    <a asp-action="Index" class="btn btn-warning">Back</a>
</div>
